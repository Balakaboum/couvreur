import { useState } from "react"; import { Loader2, Upload } from "lucide-react"; import { base44 } from "@/api/base44Client";
const initial={full_name:"",phone:"",email:"",city:"",work_type:"",message:""};
export default function QuoteForm(){const [form,setForm]=useState(initial),[file,setFile]=useState(null),[loading,setLoading]=useState(false),[sent,setSent]=useState(false),[error,setError]=useState(""),[hp,setHp]=useState(""); const change=e=>setForm({...form,[e.target.name]:e.target.value});
const submit=async e=>{e.preventDefault();if(hp){setSent(true);setForm(initial);setFile(null);return}setLoading(true);setError("");try{let photo_url="";if(file)({file_url:photo_url}=await base44.integrations.Core.UploadFile({file}));await base44.entities.QuoteRequest.create({...form,photo_url});setSent(true);setForm(initial);setFile(null)}catch{setError("Une erreur est survenue. Vous pouvez nous appeler directement.")}finally{setLoading(false)}};
if(sent)return <div className="rounded-2xl bg-blue-50 p-8 text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-full bg-[#0055FF] text-2xl text-white">✓</div><h3 className="mt-4 text-xl font-bold text-[#0A2540]">Votre demande a bien été envoyée.</h3><p className="mt-2 text-slate-600">Merci. Notre équipe reviendra vers vous dans les meilleurs délais.</p><button onClick={()=>setSent(false)} className="mt-5 text-sm font-bold text-[#0055FF]">Envoyer une autre demande</button></div>;
return <form onSubmit={submit} className="grid gap-4 sm:grid-cols-2"><input type="text" name="hp_website" value={hp} onChange={e=>setHp(e.target.value)} tabIndex={-1} autoComplete="off" aria-hidden="true" className="absolute -left-[9999px] h-0 w-0 opacity-0"/>{[["full_name","Nom / prénom","text"],["phone","Téléphone","tel"],["email","Email","email"],["city","Ville","text"]].map(([n,p,t])=><input key={n} name={n} type={t} value={form[n]} onChange={change} placeholder={p} required className="field"/>)}<select name="work_type" value={form.work_type} onChange={change} required className="field sm:col-span-2"><option value="">Type de travaux</option>{["Rénovation","Réparation / fuite","Couverture","Étanchéité","Zinguerie","Nettoyage / entretien","Gouttières","Autre"].map(x=><option key={x}>{x}</option>)}</select><textarea name="message" value={form.message} onChange={change} placeholder="Décrivez brièvement votre projet" required rows="4" className="field sm:col-span-2"/><label className="field flex cursor-pointer items-center gap-3 text-slate-500 sm:col-span-2"><Upload size={18}/><span>{file?file.name:"Ajouter une photo du chantier (optionnel)"}</span><input type="file" accept="image/*" onChange={e=>setFile(e.target.files[0])} className="sr-only"/></label>{error&&<p className="text-sm text-red-600 sm:col-span-2">{error}</p>}<button disabled={loading} className="btn-primary justify-center sm:col-span-2">{loading?<><Loader2 className="animate-spin"/> Envoi en cours...</>:"Obtenir mon devis"}</button></form>}
